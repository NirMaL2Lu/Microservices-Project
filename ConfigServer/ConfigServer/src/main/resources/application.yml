server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/NirMaL2Lu/microservice-configuration
          clone-on-start: true